#include <stdio.h>
#include <stdlib.h>
int main(void)
{
	int i = 1;
	float x, next = 1.0, e = 1.0;
	printf(" enter a value for x=");
	scanf("%f", &x);
	while (i < 100)
	{
		next = (x / i) * next;
		e = e + next;
		i++;

	}
	printf("e raised to the power %f=%.3f\n", x, e);
	return 0;
}
